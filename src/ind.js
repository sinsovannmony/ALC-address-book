function trial(not) {
    this.nay = not.nay;
    this.way = not.way;
    this.bay = not.bay;
    this.say = not.say;
}
function Contact(user) {
    this.name = user.name;
    this.imeeage = user.imeeage;
}